# Player wireframe

Visual style matches the in-app dark + neon chrome. Layout mirrors PlayerView: header, player-chrome, bingo area, FAB (shown as clipped arc when cut by viewport). Middle area may show 5x5 grid when card is sized.

Open `player-device-wireframe.svg` in a browser.
